sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t dj/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true dj/react-test npm test

after_success:
  - docker build -t dj/multi-client ./client
  - docker build -t dj/multi-nginx ./nginx
  - docker build -t dj/multi-server ./server
  - docker build -t dj/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dj/multi-client
  - docker push dj/multi-nginx
  - docker push dj/multi-server
  - docker push dj/multi-worker
